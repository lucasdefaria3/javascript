//on crée la fonction "addition" prenant en paramètres a et b et qui retourne a + b.
function addition(a, b) {
    return a + b;
}

//pour l'exemple on fait appel à la fonction "addition" avec en paramètres 14 et 6, 
//on invoque la fonction puis renvoie le resultat dans les logs
let resultat = addition(14, 6);
console.log(resultat);